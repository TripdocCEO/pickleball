# 정관 피클볼 클럽 — QA 리포트

- **일자**: 2026-07-30 (최초 2026-07-29)
- **대상**: 프런트엔드 23페이지(관리자 포함) + 백엔드 API(FastAPI) + DB(SQLite)
- **환경**: Windows 10 / Python 3.13.5 / FastAPI 0.140.13 / Starlette 1.3.1 / SQLite(WAL)
- **결과**: **자동 테스트 56건 전건 통과**, E2E 시나리오 22건 통과, 발견 이슈 5건 **모두 수정 완료**

---

## 1. 요약

| 영역 | 검사 | 결과 |
|---|---|---|
| API 자동 테스트 | 46 케이스 (pytest) | ✅ 46 pass / 0 fail |
| 게시판 E2E | 목록·상세·댓글·좋아요·검색·필터·정렬·글쓰기 | ✅ 통과 |
| 체험 신청 E2E | 검증 실패 2종 + 정상 접수 + DB 반영 | ✅ 통과 |
| 보안 | XSS · SQL 인젝션 · 인증 · 레이트리밋 | ✅ 통과 |
| 링크 무결성 | 22페이지 내부 링크 전수 | ✅ 깨진 링크 0 |
| 이미지 | 7종 전수 | ✅ 로드 실패 0 |
| 반응형 | 375px / 데스크톱 | ✅ 가로 스크롤 없음 |
| 콘솔·서버 로그 | 오류 수집 | ✅ 오류 0 |

---

## 2. 발견 이슈 및 조치

### 🔴 ISSUE-01 — 레이트리밋이 정상 사용까지 차단할 수 있음
- **발견 경위**: 페이지네이션 테스트에서 글 7건 작성 시 3건만 저장되고 나머지가 429로 거부됨.
- **분석**: 레이트리밋(분당 3건)이 하드코딩되어 있어 운영 상황에 맞춰 조정할 수 없었음. 대회 접수일처럼 글이 몰리는 날 정상 사용자가 막힐 수 있음.
- **조치**: 제한값을 환경변수로 분리 (`JGPC_RATE_POST` / `JGPC_RATE_COMMENT` / `JGPC_RATE_TRIAL`). 기본값은 유지.
- **재검증**: 제한 해제 후 7건 정상 저장, 기본값에서는 4번째 요청이 429 반환 — 양쪽 모두 통과.

### 🟡 ISSUE-02 — 체험 신청 폼의 오류 메시지가 항목마다 다르게 표시됨
- **발견 경위**: 이름을 비우고 제출하면 브라우저 기본 말풍선이 뜨고, 전화번호 형식 오류는 폼 안의 빨간 박스로 표시돼 안내 방식이 불일치.
- **분석**: `required` 속성 때문에 브라우저 기본 검증이 먼저 동작해 커스텀 검증이 실행되지 않음.
- **조치**: 폼에 `novalidate` 적용 → 모든 오류를 동일한 빨간 박스 + 해당 입력칸 강조(빨간 테두리)로 통일.
- **재검증**: 공백만 입력 시 "이름을 입력해 주세요." 표시 + 입력칸 강조 확인.

### 🔴 ISSUE-03 — 모바일에서 햄버거 메뉴 내용이 보이지 않음 *(2차 QA)*
- **증상**: 메뉴를 열면 하위 항목이 잘려 보이지 않음.
- **분석**: 헤더에 적용된 `backdrop-filter`(반투명 블러)가 **`position:fixed` 자식의 컨테이닝 블록**이 되어, 메뉴 패널이 헤더 박스(66px) 안에 갇혔습니다. 실측 결과 패널 높이가 **506px여야 할 것이 47px**로 찌부러져 있었습니다.
- **조치**: 패널을 `position:absolute` + `max-height:calc(100vh - 66px)`로 전환. 아울러 하위 메뉴를 **아코디언**으로 바꿔 스크롤 길이를 746px → 506px로 줄이고, 데스크톱 드롭다운 규칙(`transform`·`inset`·`width`)이 모바일로 새어 들어오지 못하도록 `!important`로 차단.
- **재검증**: 360px·390px에서 잘린 항목 0건, 가로 스크롤 없음, 서브메뉴 전 항목 화면 내 표시.

### 🟡 ISSUE-04 — 드롭다운이 바깥 클릭으로 닫히지 않음 *(2차 QA)*
- **분석**: 외부 클릭 핸들러가 모바일 패널만 닫고 `.nd.open` 드롭다운은 그대로 두었음.
- **조치**: `closeDropdowns()` 추가해 함께 닫도록 처리. ESC 키도 동일 동작.

### 🟡 ISSUE-05 — 버튼 글자가 줄바꿈되며 왼쪽으로 쏠림 *(2차 QA)*
- **증상**: "무료 체험 신청하기"가 두 줄로 접히면서 좌측 정렬돼 옆 버튼과 어긋나 보임.
- **분석**: `.btn`이 `inline-flex`라 `justify-content:center`는 블록 전체만 가운데 정렬하고, 내부 줄바꿈된 텍스트는 기본값(왼쪽)으로 정렬됨. 좁은 화면에서 `flex:1;min-width:150px`로 버튼 폭이 169px까지 줄어 줄바꿈이 발생.
- **조치**: `.btn`에 `text-align:center` 추가(모든 버튼 공통 해결) + 560px 이하에서 히어로 CTA를 전체 폭으로 쌓아 한 줄 유지.
- **재검증**: 390px에서 두 버튼 모두 1줄(높이 54/58px), `text-align:center` 확인.

> 다섯 이슈 모두 수정 후 재검증까지 완료했습니다. **미해결 이슈 없음.**

---

## 3. 자동 테스트 상세 (56건)

```
pytest -q  →  56 passed
```

| 그룹 | 케이스 수 | 주요 내용 |
|---|---|---|
| 헬스·기본 | 2 | 헬스체크, 빈 목록 응답 |
| 글쓰기 | 10 | 생성, 공백 트리밍, 제어문자 제거, 검증 실패 7종 |
| 조회·목록 | 8 | 조회수 증가, 404, 검색, 분류 필터, 잘못된 분류 400, 고정글 우선, 페이지네이션, 페이지 범위 |
| 댓글 | 3 | 등록, 없는 글 404, 길이 검증 |
| 좋아요 | 2 | 클라이언트별 1회 제한, 토큰 검증 |
| 관리자 | 6 | 삭제 인증, 소프트 삭제, 신청 목록·상태 변경, 통계 |
| 체험 신청 | 8 | 접수, 검증 실패 6종, 전화번호 형식 4종 허용 |
| 레이트리밋 | 2 | 글 3회/분, 신청 3회/분 |
| 보안 | 3 | XSS 원문 보존, SQL 인젝션, 정적/API 라우팅 충돌 |
| 정적 서빙 | 2 | index.html 서빙, /api 경로 미차단 |
| 관리자 페이지 API | 10 | 토큰 검증, 삭제글 포함 목록, 고정/해제, 복구, 메모 부분수정, 색인 제외 |

**검증 규칙 커버리지**
- 제목 2~60자 · 본문 2~5000자 · 닉네임 12자 · 댓글 500자
- 분류 화이트리스트(공지는 API로 작성 불가 — 운영자 전용)
- 휴대폰 정규식: `010-1234-5678`, `01012345678`, `010 1234 5678`, `011-234-5678` 허용 / `02-123-4567`, `1234` 거부
- 페이지 크기 1~50, 페이지 번호 1 이상

---

## 4. E2E 브라우저 시나리오

| # | 시나리오 | 기대 | 결과 |
|---|---|---|---|
| 1 | 게시판 진입 | 서버 연결 배지 표시, DB 글 10건 로드 | ✅ "● 서버 연결됨" |
| 2 | 글 상세 진입 | 조회수 +1, 본문·댓글 표시 | ✅ |
| 3 | 좋아요 | 1 증가 후 재클릭해도 유지 | ✅ 1 → 1 |
| 4 | 댓글 등록 | 카운트 +1, 즉시 반영 | ✅ |
| 5 | 목록 복귀 | 리스트 재조회 | ✅ |
| 6 | 검색 "키친" | 1건 | ✅ |
| 7 | 파트너 탭 | 2건 | ✅ |
| 8 | 조회순 정렬 | 고정 공지가 여전히 최상단 | ✅ |
| 9 | 글쓰기 | DB 저장, 총계 10→11, 토스트 노출 | ✅ |
| 10 | 레이트리밋 | 4번째 글에서 429 안내, **모달 유지로 작성 내용 보존** | ✅ |
| 11 | 체험 신청 | 검증 2종 차단 후 정상 접수, 버튼 비활성(중복 방지) | ✅ |
| 12 | 신청 DB 확인 | 관리자 API에 이름·연락처·희망시간·인원·경험·유입경로 전부 기록 | ✅ |
| 13 | 모바일 메뉴 (360·390px) | 잘림 0건, 가로 스크롤 없음, 아코디언 동작 | ✅ |
| 14 | 모바일 메뉴 이동 | 햄버거 → 커뮤니티 → 대회 클릭 시 이동 + 메뉴 자동 닫힘 | ✅ |
| 15 | 드롭다운 배타성 | 다른 카테고리 열면 이전 것 닫힘, 바깥클릭·ESC로 닫힘 | ✅ |
| 16 | 버튼 정렬 (390px) | 두 CTA 모두 1줄, 가운데 정렬 | ✅ |
| 17 | 관리자 로그인 | 잘못된 토큰 차단, 정상 토큰 진입, 세션 저장 | ✅ |
| 18 | 관리자 통계 | 신규/7일/누적/게시판 4개 카드 정확 | ✅ |
| 19 | 신청 상태 변경 | 즉시 저장 + 신규 카운트 3→2 자동 반영 | ✅ |
| 20 | 신청 메모 저장 | blur/Enter 저장, 상태와 독립적으로 유지 | ✅ |
| 21 | CSV 내보내기 | UTF-8 BOM(EF BB BF) 확인 — 엑셀 한글 정상 | ✅ |
| 22 | 게시글 관리 | 고정→해제→삭제→복구 전 과정 동작 | ✅ |

---

## 5. 보안 점검

| 항목 | 방식 | 결과 |
|---|---|---|
| **저장형 XSS** | `<img src=x onerror=...>` + `<script>` 본문으로 글 등록 후 목록·상세 렌더링 | ✅ **미실행**. 본문 DOM 요소 노드 0개, 목록 제목은 `&lt;b&gt;`로 이스케이프. 서버는 원문 보존, 출력은 `textContent`로 처리 |
| **SQL 인젝션** | 검색어 `' OR 1=1 --` | ✅ 결과 0건 반환, 테이블 정상 — 전 쿼리 파라미터 바인딩 |
| **관리자 인증** | 토큰 없음 / 잘못된 토큰 / 정상 토큰 | ✅ 401 / 401 / 200 |
| **레이트리밋** | 분당 초과 요청 | ✅ 429 + 한국어 안내 |
| **개인정보** | 접속 IP 처리 | ✅ 원문 미저장, 솔트 적용 SHA-256 앞 16자만 기록 |
| **삭제 정책** | 글 삭제 | ✅ 소프트 삭제(`deleted=1`) — 목록·상세에서 제외되며 복구 가능 |
| **CORS** | 허용 오리진 | ✅ 화이트리스트 방식(기본 localhost) |

### 운영 배포 전 반드시 조치할 것
1. `JGPC_ADMIN_TOKEN`을 긴 랜덤 문자열로 교체 (기본값 `changeme-dev-token` 사용 금지)
2. `JGPC_IP_SALT` 교체
3. `JGPC_ORIGINS`를 실제 도메인으로 지정
4. HTTPS 적용 (리버스 프록시)
5. `data/club.db` 정기 백업 — SQLite는 파일 복사로 백업 가능

---

## 6. 알려진 제약 (의도된 설계)

| 항목 | 현재 | 사유 / 향후 |
|---|---|---|
| 회원 로그인 | 없음 (닉네임 입력) | 기획서 §09 "회원 200명 전까지 만들지 않음" 원칙. 필요 시 NextAuth·소셜로그인 추가 |
| 글 수정·삭제 (작성자) | 관리자만 삭제 가능 | `posts.author_key`(IP 해시) 컬럼을 미리 확보해 둠 — 본인 확인 기능 추가 시 사용 |
| 이미지 첨부 | 미지원 | 스토리지 정책 확정 후. 현재는 텍스트 전용 |
| 페이지네이션 UI | 최대 50건 로드 | 글 100건 넘어가면 더보기 버튼 추가 권장 |
| 관리자 화면 | `/admin.html` 제공 | 토큰 인증. 계정 체계가 필요해지면 로그인 도입 |
| 관리자 접근 통제 | URL은 공개, API가 토큰으로 보호 | 페이지 자체는 열리지만 토큰 없이는 데이터가 표시되지 않음. 더 강한 통제가 필요하면 리버스 프록시에서 IP 제한 또는 Basic Auth 권장 |
| DB | SQLite | 동시 접속 수백 명까지 충분. 그 이상이면 PostgreSQL로 이전(스키마 호환) |

---

## 7. 재현 방법

```bash
# 테스트
cd app
.venv\Scripts\python.exe -m pytest -v

# 서버 실행
run.bat
# → 사이트 http://localhost:8000  /  API 문서 http://localhost:8000/api/docs
```
